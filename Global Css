/* General Body */
body {
    background: url('HomePagePic3.jpg') no-repeat center center fixed;
    background-size: cover;
    image-rendering: crisp-edges; /* Ensures sharpness */
    image-rendering: -webkit-optimize-contrast;
    font-family: 'Arial', sans-serif; /* Default font family */
    margin: 0;
    padding: 0;
    color: #333;
}

/* Header (Navigation Bar) */
#home_header {
    background: rgba(204, 230, 240, 0.9); /* Slight transparency */
    padding: 20px 40px;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    display: flex;
    justify-content: center; /* Center the contents horizontally */
    align-items: center; /* Center vertically */
    z-index: 1000;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Added shadow for depth */
    backdrop-filter: blur(10px); /* Blurred background for a sleek look */
}

/* Logo (Left Side) */
#home_logo {
    color: rgb(4, 72, 136);
    font-size: 30px;
    font-weight: bold;
    text-transform: uppercase; /* Adds a professional touch */
    letter-spacing: 2px; /* Slightly spaced letters */
    margin-right: auto; /* Pushes logo to the left */
}

/* Navigation Menu (Right Side) */
#home_nav {
    display: flex;
    justify-content: center; /* Center the links horizontally */
    align-items: center; /* Center the links vertically */
    flex-grow: 1;
}

/* Navigation Links */
#home_nav-links {
    list-style: none;
    display: inline-flex;
    gap: 20px; /* Increased space between links */
    margin: 0;
    padding: 0;
}

#home_nav-links a {
    text-decoration: none;
    color: rgba(11, 120, 192, 0.9);
    font-size: 18px;
    font-weight: 500; /* Added weight for better legibility */
    text-transform: uppercase;
    transition: color 0.3s ease, transform 0.3s ease;
}

#home_nav-links a:hover {
    color: #1d08dd;
    transform: translateY(-2px); /* Slight hover effect */
}

/* Hero Section */
#home_hero {
    height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
    padding: 60px 20px; /* Adjusted padding for a balanced look */
    margin-top: 80px; /* Increased space to avoid header overlap */
    background: rgba(0, 0, 0, 0.5); /* Slight overlay for text readability */
}

/* Hero Title */
#home_hero-title {
    font-size: 60px;
    margin-bottom: 20px;
    color: #00bcd4; /* Lighter, more modern blue */
    font-weight: 700;
    text-transform: uppercase;
}

/* Hero Text */
#home_hero-text {
    font-size: 22px;
    margin-bottom: 30px;
    color: #a0e4f7;
    font-weight: 400;
}

/* Button */
#home_btn {
    display: inline-block;
    background: #00bcd4;
    color: #fff;
    padding: 12px 24px;
    font-size: 18px;
    font-weight: 600;
    text-decoration: none;
    border-radius: 5px;
    transition: background 0.3s, transform 0.3s ease;
    text-transform: uppercase;
}

#home_btn:hover {
    background: #007f91;
    transform: scale(1.05); /* Slight scaling effect */
}

/* Footer */
#home_footer {
    background: #03bef7;
    color: white;
    text-align: center;
    padding: 20px 0;
    margin-top: 50px; /* Increased margin for a balanced layout */
    font-size: 14px;
    font-weight: 400;
}

#home_footer a {
    color: #ffffff;
    text-decoration: none;
    font-weight: 600;
    transition: color 0.3s ease;
}

#home_footer a:hover {
    color: #1d08dd;
}

/* Responsive Design */
@media (max-width: 768px) {
    #home_header {
        flex-direction: column;
        align-items: flex-start;
        padding: 15px 30px;
    }

    #home_nav-links {
        flex-direction: column;
        gap: 15px;
        padding-top: 20px;
        align-items: center;
    }

    #home_hero-title {
        font-size: 45px;
    }

    #home_hero-text {
        font-size: 18px;
    }

    #home_btn {
        padding: 10px 20px;
        font-size: 16px;
    }

    #home_footer {
        font-size: 12px;
    }
}
